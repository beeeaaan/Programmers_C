#include <bits/stdc++.h>
using namespace std;

int solution(vector<int> nums)
{
    // unordered_set<[변수타입]> [변수명] : 정렬되지 않은 set 
    unordered_set<int> s(nums.begin(), nums.end());
    return min(nums.size() / 2, s.size());
}
