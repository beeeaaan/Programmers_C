#include <string>
#include <vector>

using namespace std;

int solution(vector<int> numbers) {
    int answer = 45;
    
    for(int n : numbers)
        answer -= n;  // numbers에 있는 숫자 전부 빼기
    
    return answer;
}
