#include <string>
#include <vector>
#include <algorithm>
using namespace std;

vector<int> solution(vector<int> arr) {
    arr.erase(min_element(arr.begin(), arr.end()));  // 가장 작은 수 제거
    
    if (arr.empty()) return {-1};  // 비어있으면 -1
    return arr;
}
