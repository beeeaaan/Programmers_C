#include <string>

using namespace std;

int solution(int a, int b) {
    string x = to_string(a) + to_string(b);
    string y = to_string(b) + to_string(a);
    
    if(stoi(x) >= stoi(y)) return stoi(x);  // stoi : 문자열을 숫자로 변경
    else return stoi(y);
}
