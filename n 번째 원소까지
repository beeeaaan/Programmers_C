#include <vector>

using namespace std;

vector<int> solution(vector<int> num_list, int n) {
    vector<int> answer;
    
    // 첫 번째 원소부터 n 번째 원소까지
    for(int i = 0; i < n; ++i) answer.push_back(num_list[i]);
    return answer;
}
