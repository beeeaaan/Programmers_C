#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<string> strlist) {
    vector<int> answer;
    
    // 각 문자열 길이 계산
    for(const string&str : strlist) {
        answer.push_back(str.length());
    }
    return answer;
}
