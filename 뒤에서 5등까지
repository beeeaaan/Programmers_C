#include <vector>
#include <algorithm>

using namespace std;

vector<int> solution(vector<int> num_list) {
    sort(num_list.begin(), num_list.end());  // 정렬
    vector<int> answer(num_list.begin(), num_list.begin() + 5);  // 5등까지
    return answer;
}
