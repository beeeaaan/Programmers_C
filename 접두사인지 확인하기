#include <string>
#include <vector>

using namespace std;

int solution(string my_string, string is_prefix) {
    int answer = 0;
    if (my_string.find(is_prefix) == 0) {  // find :  문자열에서 특정 문자열 위치 찾기
        answer = 1;
    }
    return answer;
}
