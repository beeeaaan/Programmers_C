#include <string>
#include <vector>

using namespace std;

string solution(string my_string, string letter) {
    string answer = "";
    
    for(int i = 0; i < my_string.size(); i++)
        if(my_string[i] != letter[0]) answer += my_string[i];  // 특정 문자 아닐 경우 answer에 추가

    return answer;
}
