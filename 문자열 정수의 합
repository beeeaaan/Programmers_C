#include <string>

using namespace std;

int solution(string num_str) {
    int answer = 0;
    
    for(int i = 0; i < num_str.size(); ++i) answer += num_str[i] - '0';  // 각 자리의 문자 형태의 숫자에서 '0'을 빼서 int로 변환
    return answer;
}
