#include <string>
#include <vector>

using namespace std;

int solution(vector<int> box, int n) {
    int answer = 0;
    
    // 주사위 최대 개수(가로, 세로, 높이를 주사위의 모서리 길이로 나누고 곱하기)
    answer=(box[0] / n) * (box[1] / n) * (box[2] / n);
    
    return answer;
}
