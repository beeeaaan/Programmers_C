#include <string>
#include <vector>

using namespace std;

string solution(string my_string) {
    string answer = "";
    
    for(auto& s : my_string) {
        if(s != 'a' && s != 'e' && s != 'i' && s != 'o' && s != 'u')  // 모음 알파벳 제거
            answer += s;
    }    
    return answer;
}
