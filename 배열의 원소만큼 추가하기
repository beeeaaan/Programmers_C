#include <string>
#include <vector>

using namespace std;

vector<int> solution(vector<int> arr) {
    vector<int> answer;
    
    for(const auto& a : arr){
        answer.insert(answer.end(), a, a);  // 객체.insert(위치, 문자열) : 특정 위치 문자열 삽입
    }
    return answer;
}
