#include <stdio.h>
#define LEN_INPUT 11

int main(void) {
    char s1[LEN_INPUT];
    int a;
    // 입력
    scanf("%s %d", s1, &a);
    // 반복 출력
    for(int i = 1; i <= a; i++) {
        printf(s1);
    }
    return 0;
}
