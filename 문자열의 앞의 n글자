#include <string>

using namespace std;

string solution(string my_string, int n) {
    // substr(시작인덱스, 문자열 길이) : 시작인덱스 지정한 문자열 길이만큼 추출
    string answer = my_string.substr(0, n);
    return answer;
}
