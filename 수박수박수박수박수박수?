#include <string>
#include <vector>

using namespace std;

string solution(int n) {
    string answer = "";
    
    for(int i=0;i<n;i++) {
        // 짝수일 경우
        if(i % 2 == 0)
            answer += "수";
        // 홀수일 경우
        else
            answer += "박";
    }
    
    return answer;
}
