#include <string>
#include <vector>

using namespace std;

string solution(string my_string, string alp) {
    string answer = my_string;
    
    for(char& c : answer) {
        if(c == alp[0]) {  // alp에 해당하는 글자
            c =toupper(c);  // 대문자로 바꾸기
        }
    }
    return answer;
}
