#include <vector>

using namespace std;

int solution(vector<int> arr, int idx) {
    // 배열의 값이 1인 가장 작은 인덱스 찾기
    for(int i = idx; i < arr.size(); ++i) {
        if(arr[i] == 1) return i;
    }
    return -1;
}
