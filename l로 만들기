#include <string>
#include <vector>

using namespace std;

string solution(string myString) {   
    for(char& c : myString) {
        // 'l'보다 앞서는 모든 문자 'l'로 바꾸기
        if(c < 'l') {
            c = 'l';
        }
    }
    return myString;
}
