#include <string>
#include <vector>

using namespace std;

vector<int> solution(long long n) {
    vector<int> answer;
    
    while(n) {
        answer.push_back(n % 10);  // 뒤집기 위해 나머지 계산 반복
        n /= 10;
    }
    return answer;
}
