#include <vector>

using namespace std;

vector<int> solution(int start_num, int end_num) {
    vector<int> answer;
    
    for(int i = start_num; i >= end_num; --i) {  // start_num에서 end_num까지 1씩 감소
        answer.push_back(i);
    }
    return answer;
}
