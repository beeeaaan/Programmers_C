#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

vector<int> solution(vector<int> arr) 
{
    vector<int> answer;

    // unique : vecter 내 중복 원소 제거
    arr.erase(unique(arr.begin(), arr.end()), arr.end());
    answer = arr;
    
    return answer;
}
