#include <string>
#include <vector>

using namespace std;

string solution(string my_string, int k) {    
    return [&]{
        string answer = "";
        
        // k번 반복
        while(k--) {
            answer += my_string;
        }
        return answer;
    } ();
}
