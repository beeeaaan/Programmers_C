#include <string>
#include <vector>
#include <regex>

using namespace std;

string solution(string rny_string) {
    // regex_replace(문자열, regex(정규식), 치환할문자열) : 문자열에서 문자 검색해 치환
    string answer = regex_replace(rny_string, regex("m"), "rn"); 
    return answer;
}
