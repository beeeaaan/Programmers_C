#include <string>
#include <vector>

using namespace std;

string solution(int num) {
    string answer = "";
    
    if(num % 2 == 0) {  // 짝수일 경우
        answer = "Even";
    } else if(num % 2 != 0) {  // 홀수일 경우
        answer = "Odd";
    }
    return answer;
}
