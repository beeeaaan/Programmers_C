#include <string>
#include <vector>
#include <algorithm>

using namespace std;

string solution(string my_string) {
    string answer = "";
    
    // 소문자 변환
    for(auto c:my_string) {
        answer += tolower(c);
    }
    // 정렬
    sort(answer.begin(), answer.end());
    
    return answer;
}
