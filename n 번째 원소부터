#include <vector>

using namespace std;

vector<int> solution(vector<int> num_list, int n) {
    vector<int> answer;
    
    // 초기값(n번째 원소 vector 내 index n - 1이므로)
    for(int i = n - 1 ; i < num_list.size(); ++i) 
        answer.push_back(num_list[i]);
    
    return answer;
}
