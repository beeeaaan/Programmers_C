#include <vector>

using namespace std;

int solution(vector<int> num_list) {
    int answer = -1;
    
    for(int i = 0; i < num_list.size(); ++i) {
        if(num_list[i] < 0) return i;  // 첫번째로 나오는 음수
    }
    return answer;
}
