#include <string>
#include <algorithm>

using namespace std;

string solution(string myString) {
    // tolower : 소문자 변환
    transform(myString.begin(), myString.end(), myString.begin(), ::tolower);
    return myString;
}
